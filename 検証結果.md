# 試用パックの検証

2026-09-07。外部公開なし。

- PowerShell 7をPATHの`pwsh`から実行。
- `-InboxRoot`で同梱sampleを指定したSnapshotが終了コード0。
- InboxRootを省略した既定Snapshotも終了コード0。指定時と同一出力。
- 表示は架空のdemo_001_request.md、demo_002_reply.md、demo_003_archive.mdの3件だけ。
- 配置済み・既読記録あり・保管済みの3状態を確認。既読ログ自体も架空。
- パック内の固定ユーザーディレクトリ、個人名、実共有フォルダ名、メール形式、一般的なトークン/秘密鍵形式を文字列検査し一致0。完全な秘密検出を保証する検査ではないが、元の受信箱や実メッセージはコピーせずサンプルを新規作成した。
- 検証前後で元のコンソールのSHA256が一致。元ファイルへは書き込んでいない。
- Start.cmdはPATHのPowerShell 7を呼び、同梱sampleを明示指定する。実行ポリシー変更・AI起動・外部通信の命令は追加していない。

V/N/R/Qの対話操作とStart.cmdのダブルクリックは未実機確認。Nは元のローカルファイル作成処理を残し、宛先が同梱sampleになる構成を読解確認した。Snapshotは書き込みを行わない経路で実行済み。

ZIP化前のディレクトリとして引渡し。利用者が独自の-InboxRootを指定した場合までサンプルに隔離する設計ではなく、READMEにその制約を明記した。

## 本部追加検証 2026-09-07

上記は初回引渡し時点の記録。後続のPTY試験で日本語一覧、V→1の全文、Enterで戻る、N→空宛先の取消、Q終了（exit0）を確認済み。送信実関数は隔離したサンプルで両者への同一ID配送・空本文取消を別途検証。GUIダブルクリック、他の利用者のPC、AI連携は未検証です。
